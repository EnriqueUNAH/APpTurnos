<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Usuarios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            color: white;
            display: flex;
            justify-content: center;
            padding: 20px;
        }

        .container {
            max-width: 80%;
            margin: 0 auto;
            padding: 20px;
        }

        .user-cards {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            border-block-color: white;
        }

        .user-card {
            background-color: #36393f;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 300px;
            margin-bottom: 1rem;
            transition: transform 0.3s, box-shadow 0.3s; /* Agregar efecto de transición */
        }

            .user-card:hover {
                transform: translateY(-10px); /* Efecto de elevación */
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4); /* Efecto de sombra */
            }

        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .form-label {
            color: black;
        }

        .filter-section-row {
            display: flex;
            gap: 10px;
            align-items: center;
        }
    </style>

</head>
<body>
    <div class="row">
        <div class="col-md-12">
            <div class="title">
                <div class="title-body">
                    <div class="filter-section-row">
                        <input type="text" id="filterNombre" class="form-control" placeholder="Nombre de usuario">
                        <select id="filterArea" class="form-control">
                            <option value="">Todas las Áreas</option>
                            <option value="1">SERVIDORES</option>
                            <option value="2">REDES Y COMUNICACIÓN</option>
                            <option value="3">DESARROLLO</option>
                            <option value="4">USUARIOS EXPERTOS</option>
                            <option value="5">SOPORTE TECNICO</option>
                            <option value="6">INFRAESTRUCTURA Y TALLER</option>
                            <option value="7">PROYECTOS</option>
                            <option value="8">JEFATURAS</option>
                        </select>
                        <select id="filterZona" class="form-control">
                            <option value="">Todas las Zonas</option>
                            <option value="1">Centro Sur</option>
                            <option value="2">Norte</option>
                            <option value="3">Occidente</option>
                        </select>
                    </div>
                    <div>
                       <p></p>
                    </div>
                   
                    <div id="user-cards" class="user-cards">
                        <!-- Tarjetas de usuario se agregarán aquí dinámicamente -->
                    </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/Views/Cliente.js"></script> <!-- Incluye tu archivo de scripts -->
</body>
</html>
